# Personal access token used by the GitHub stats route (app/api/github/route.ts).
# Needs no scopes for public data; classic token with `public_repo` is plenty.
# Create one at https://github.com/settings/tokens
GITHUB_PAT=
